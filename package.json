{
  "name": "@ds.e/core",
  "version": "1.0.0",
  "description": "my persona",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx lerna run build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^7.3.1"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ]
}
